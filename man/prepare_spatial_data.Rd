% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inteRpeval.R
\name{prepare_spatial_data}
\alias{prepare_spatial_data}
\title{Prepare Spatial Data and Voronoi Raster}
\usage{
prepare_spatial_data(
  points_data,
  lon_col,
  lat_col,
  boundary,
  dep_var,
  input_crs = "+proj=longlat +datum=NAD83",
  target_crs =
    "+proj=aea +lat_1=34 +lat_2=40.5 +lat_0=0 +lon_0=-120\\n                  +x_0=0 +y_0=-4000000 +datum=WGS84 +units=m",
  raster_res = 10000
)
}
\arguments{
\item{points_data}{A \code{data.frame} containing coordinates and attributes.}

\item{lon_col}{Character. Name of the longitude column in \code{points_data}.}

\item{lat_col}{Character. Name of the latitude column in \code{points_data}.}

\item{boundary}{A \code{SpatVector} polygon defining the study area boundary.}

\item{dep_var}{Character. Name of the target/response variable column.}

\item{input_crs}{Character. CRS of the input points. Defaults to NAD83
geographic (\code{"+proj=longlat +datum=NAD83"}).}

\item{target_crs}{Character. CRS to project data into. Defaults to California
Albers Equal Area.}

\item{raster_res}{Numeric. Raster resolution in map units (metres). Default
is \code{10000}.}
}
\value{
A named list with the following elements:
\describe{
  \item{dta}{Projected \code{SpatVector} of input points.}
  \item{cata}{Projected \code{SpatVector} of the boundary polygon.}
  \item{r}{Empty template \code{SpatRaster} at the specified resolution.}
  \item{vr}{Voronoi-rasterized \code{SpatRaster} of the response variable.}
  \item{df}{A \code{data.frame} of projected coordinates and attributes for use with \code{gstat}.}
  \item{dep_var}{Sanitised name of the response variable (via \code{make.names}).}
}
}
\description{
Cleans and reprojects point data, builds a Voronoi polygon raster, and
returns all spatial objects needed for interpolation functions.
}
